# HANDOFF — Session Checkpoint

**Last session:** 2026-05-06.
**Hand-off by:** Claude (Opus 4.7, §4.7+R11 sub-pass R1 scope round).
**Hand-off to:** next Claude Code session after `/clear`.

---

## TL;DR

**§4.7 enemy direction + R11 wave-variance sub-pass R1 SCOPE LANDED** ([`decisions/2026-05-06-enemy-direction-r1-scope.md`](decisions/2026-05-06-enemy-direction-r1-scope.md), commit `23ad8b5`, dual-pushed). PM picked all Recommended on the 3-question batched gate: per-property axis / R11 bundled / dual-push after R1 + each content round + RN.

**4-round queue locked.** R1 scope (DONE) → R2 Direction A/B/C lock + body → R3 R11 wave-composition variance grammar → R4 RN cross-arc audit + spine-doc edits at `concept/phase-4.md §4.7` + `§4.8` exit-gate item #4 ✅ tick. 3-property scope: (1) Direction A/B/C lock; (2) R11 variance grammar; (3) RN cross-arc audit. PM autonomy mandate carries — R2 onward autonomous.

**NEXT session: produce R2 Direction A/B/C lock + body.** Leading evidentiary streams converge on Option C (civ-matched bosses + shared regular-wave pool): the 2026-04-27 historic-arc table presupposes C-shaped framing (round-30 antagonists Xerxes / Tlaxcalan→Tezcatlipoca / Jörmungandr); per-civ RN signature_creep_types cluster boss-tier at civ-distinct armor classes; Follow-up #5 cultural-sensitivity hard-gate disfavors Option B's three-times-the-art-burden. R2 ratifies + bodies-out (or forks to A/B with explicit reconciliation against the three streams).

---

## State snapshot

### Git
- Branch: **`session/2026-04-25-q2-world-pitch`**.
- Latest commits: `23ad8b5` (§4.7 R1 scope LANDED) → `a29756a` (handoff SHA fill) → `0b7160e` (post-per-civ-arc ratifications + handoff bundle) → `a98a5d3` (Per-civ RN LANDED).
- All commits dual-pushed to session branch + main. Local in sync with origin.
- **Carried git debt:** `49c4c54` contains 69MB `.accord/events.jsonl` blob (>50MB GitHub warning). Removing requires destructive force-push — only on explicit PM authorization.

### §4.7+R11 sub-pass status (R1 LANDED, R2-R4 PENDING)
- **R1 scope LANDED** (`23ad8b5`). 3-property scope locked. 3x debug loop on axis ran inline (Loop-1 critique: per-property fragments cross-property coherence + brittle to non-C R2 outcome; Loop-2 steelman: RN audit is integration deliverable + 3 evidentiary streams converge on Option C + variance-grammar falsification gate is direction-independent; Loop-3 synthesis: Axis A with embedded cross-property coherence hook paragraphs at close of R2/R3).
- **R2 PENDING** — Direction A/B/C lock + body. Civ-matched boss cadence reading against 2026-04-27 historic-arc table. Shared regular-wave pool armor-tag distribution targets at 5-class grain. Civ-neutral effect-type language. Cross-property coherence hook to R3 R11 grammar.
- **R3 PENDING** — Round 11 wave-composition variance grammar. Random-seeded armor-tag mix per wave + per-map crystal-lock variance + locked-content-skeleton-boss exemption. Falsifiable against §4.11.8 three-axis defense (matchup-correctness / placement-coverage / ability-uptime). Cross-property coherence hook to RN.
- **R4 RN PENDING (CLOSES arc)** — cross-arc audit (3-axis: per-civ RN matchup_affinity coherence / per-tower RN type-coverage coherence / §4.11.8 three-axis defense) + `phase-4.md §4.7` spine-doc edits (Option lock + body merged into existing direction sketches; R11 subsection body authored from R3) + `§4.8` exit-gate item #4 ✅ tick + CASCADE/PROGRESS/HANDOFF doc-hygiene.
- Dual-push points (locked): after R1 (DONE) / after R2 / after R3 / after RN. 4 dual-push events total.

### Doc-hygiene (this session)
- `PROGRESS.md` session log: 3 most-recent entries (§4.7 R1 / Ratifications / RN); R4 Norse archived to [`PROGRESS-archive.md`](PROGRESS-archive.md).
- `CASCADE.md` pointer: 1 most-recent block (§4.7 R1 scope LANDED).
- `CASCADE.md` decisions table: 80 one-line hooks (R1 scope row added).
- `CASCADE.md` version footer: 2 most-recent entries (0.73 + 0.72); 0.71 archived to [`CASCADE-history.md`](CASCADE-history.md).
- cascade-lint clean.

### Locked carry-forward (untouched this session)
- 17-item conceptual frame (a)-(r) + extension (s) — Accepted.
- All 10 Numbers-phase magnitudes — Accepted.
- 6-mode ontology — Accepted.
- Auxiliary economy structure — Accepted.
- "Go big, no scope cuts" doctrine — Accepted (hub Non-negotiable).
- 2026-04-25 locked content skeleton — Accepted.
- §5.4 [LOCKED] (Civilizations row); §2.4a [LOCKED] (accessibility floor).
- All R1-R7 CONCEPT amendment-pass §-anchors — Accepted.
- All per-commander R1-R5 spine-doc edits + lane locks (Leonidas=Control / Montezuma II=Economy / Ragnar=Summon, **Hard reversibility**) — Accepted.
- All per-tower R1-RN magnitude matrix + 4 locks (45/45 zero cascade-violations) — Accepted.
- Per-civ R1+R2+R3+R4+RN profiles + 5-field schema — Accepted.
- **Three-civ-three-equation-form fingerprint** — Phase-4-spec-level invariant.
- 2026-04-26 attack-type lock (5-armor / 7-attack / RPS / status-proc) — Accepted.

### Two prototype findings still queued (not in scope)
- **(a)** `prototype/index.html:3310` — `openReference` throws `ReferenceError: CIVS_DATA is not defined` when `game.commander.civ` is falsy.
- **(b)** `prototype/index.html:495,507,513` — mode-select copy still references "three ages" / "11-age arc". Obsolete under post-2026-04-25 4-tier ladder.

### Open follow-ups
- **#5** — Cultural-sensitivity pass. Scheduled at Phase-4 exit per 2026-05-06 post-arc ratifications item 4. High-vigilance at Aztec; moderate at Norse; standard at Greek. **Hard-gates art / VFX / civ-flavor surface direction prose during this arc** — mechanical-content only.
- **#6** — Patch-1 commanders per civ + Thor recipe-layer dissonance.
- **#7** — Foresight-coin / PvE campaign / AGES / leveling / attributes.
- **#8 / #9** — non-boss enemy ontology / additional commanders.
- **C7.b deferred** — Builder concurrency cap + 90% refund-on-cancel UI.
- **`research/06-tw-subgenres.md`** new stub.

### Post-§4.7-arc roadmap
1. §4.4 mobile-unit-control (Phase 4 OPEN BLOCKER, exit-gate item #3) — next track after §4.7+R11 closes.
2. Phase 4 numerics ratification (Fusion + §4.6 economy details).
3. Per-map authoring (good-cell + crystal-lock variance, downstream of §4.7 + Round 11).
4. Monetization specifics / engine choice lock (Godot 4 leading) / art director scope.
5. Cultural-sensitivity Follow-up #5 consultation engagement.
6. Phase 5 readiness gate (telemetry + wave variance scaffolding) — opens only after Phase 4 exits cleanly.
7. `research/06-tw-subgenres.md` stub.
8. Follow-ups #6/#7/#8/#9 + C7.b deferred.

---

## Cadence rules carried forward

- Cadence exists to manage the context window, not to gate every step.
- **PM autonomy mandate:** within established multi-round arcs where PM has consistently picked Recommended, proceed autonomously. R2-R4 of this arc run autonomously; surface AskUserQuestion only on genuine forks (e.g., R2 considers forking from C to A/B; R3 considers a variance grammar that re-opens §4.11.8 thresholds) / scope expansion / cascade-violation risk / cultural-sensitivity surface (Follow-up #5) / handoff trigger.
- AskUserQuestion is the standard interface when gating is needed. Recommended-first.
- MD-first preservation — for any scope expansion or non-trivial conceptual ratification, land in MD before further questions.
- Dual-push discipline — push to BOTH session branch AND `main` after every commit.
- **Targeted `git add` only** — never `git add -A`.
- Local-main hygiene — `git fetch origin && git log --oneline HEAD..origin/main` BEFORE reading docs.
- 3x debug loop on any CONCEPT-constraint-touching proposal.
- Doc hygiene per compression standard in [`CLAUDE.md`](CLAUDE.md) — round summaries are 1-line hooks in CASCADE/PROGRESS; full content lives in the decision file only.

---

## Next-session prompt (copy-paste after `/clear`)

```
Resuming Ash to Altar. §4.7 enemy direction + R11 wave-variance sub-pass
R1 SCOPE LANDED. 4-round per-property queue locked. R2 next.

LANDED COMMITS (all dual-pushed):
  b0d7841  per-civ R1 scope
  49c4c54  per-civ R2 Greek (+ cleanup 78cba67 untracking .accord/)
  7c3f338  per-civ R3 Aztec
  5f58829  per-civ R4 Norse
  a98a5d3  per-civ RN audit + arc close + spine-doc edits
  0b7160e  post-arc ratifications + handoff bundle
  a29756a  handoff SHA fill
  23ad8b5  §4.7+R11 R1 scope LANDED (HEAD)

KNOWN GIT DEBT (carried): commit 49c4c54 has 69MB .accord/events.jsonl
blob in history. Cleanup 78cba67 removes from tracking + .gitignore.
Full purge needs destructive force-push — DO NOT execute without
explicit PM authorization.

§4.7+R11 SUB-PASS R1 SCOPE (decisions/2026-05-06-enemy-direction-r1-
scope.md, Accepted, Medium):
  Round-queue axis: per-property (Axis A). 4 rounds.
  3-property scope locked:
    1. Direction A/B/C lock + body (R2)
    2. Round 11 wave-composition variance grammar (R3)
    3. Cross-arc audit + spine-doc edits (R4 RN)
  R11 bundled into this arc per 2026-05-06 post-arc ratification item 1.
  Dual-push points: after R1 (DONE) / R2 / R3 / RN.
  PM autonomy mandate carries — R2 onward autonomous.

LOCKED INVARIANTS (carry-forward, do not silently edit):
  - 17-item conceptual frame (a)-(r) + extension (s)
  - All 10 Numbers-phase magnitudes
  - 6-mode ontology + auxiliary economy
  - 2026-04-25 locked content skeleton
  - §5.4 [LOCKED] / §2.4a [LOCKED]
  - All R1-R7 CONCEPT amendment-pass §-anchors
  - 2026-04-26 attack-type lock (5-armor / 7-attack / RPS / status-proc)
  - Per-commander R1-R5 lane locks (Hard reversibility):
      Greek=Leonidas (Control)
      Aztec=Montezuma II (Economy)
      Norse=Ragnar Lothbrok (Summon)
  - Per-tower R1-RN 45-tower magnitude matrix + 4 locks
  - Per-civ R1+R2+R3+R4+RN profiles + 5-field schema
  - Three-civ-three-equation-form fingerprint (Phase-4-spec-level
    invariant): Greek deceleration-weighted / Aztec kill-multiplication
    -weighted / Norse summon-cleave-propagation-weighted

BOOTSTRAP per CLAUDE.md: README → CLAUDE → CASCADE → HANDOFF → PROGRESS
→ CONCEPT (admin/concept.json retired from reading order 2026-05-06).

BEFORE reading docs:
  git fetch origin
  git log --oneline HEAD..origin/main   # should be empty

PRIMARY DIRECTIVE — produce R2 Direction A/B/C lock + body.

  Three evidentiary streams converge on Option C (civ-matched bosses
  + shared regular-wave pool):
    1. concept/phase-4.md §4.7 has "Leading direction (placeholder, not
       ratified): Option C — hybrid" already written in.
    2. 2026-04-27 historic-arc table (round-30 antagonists Xerxes /
       Tlaxcalan→Tezcatlipoca / Jörmungandr) presupposes C-shaped
       framing — civ-matched bosses are already locked content-skeleton.
    3. Per-civ RN signature_creep_types cluster boss-tier at civ-
       distinct armor classes (Greek=Colossal+Shielded / Aztec=Beast+
       Spirit+Unarmored / Norse=Shielded+Unarmored).
  Plus: Cultural-sensitivity Follow-up #5 hard-gate disfavors Option B
  three-times-the-art-burden.

  R2 job: ratify Option C (or fork to A/B with explicit reconciliation
  against all three streams + Follow-up #5). Body authors:
    - Civ-matched boss cadence (mini at 5/15/25, main at 10/20/30)
      reading against the 2026-04-27 historic-arc table.
    - Shared regular-wave pool armor-tag distribution targets at the
      5-class grain (Unarmored / Shielded / Beast / Spirit / Colossal).
    - Civ-neutral effect-type language. NO per-creep-row roster
      authoring. NO art / VFX / civ-flavor surface direction prose
      (Follow-up #5 hard-gate).
    - Cross-property coherence hook paragraph at close → R3 R11
      grammar will author against the locked direction's space.

§4.7 R2 INPUTS (Grep > full-file Read):
  decisions/2026-05-06-enemy-direction-r1-scope.md (R1 scope; this arc)
  concept/phase-4.md §4.7 (current OPEN status + Option A/B/C placeholder
    + 2026-04-27 historic-arc table)
  decisions/2026-04-26-attack-type-mapping.md (5-armor + 7-attack
    taxonomy + RPS matrix — read-only)
  decisions/2026-05-06-per-civ-rn-cross-civ-audit-and-arc-close.md
    (signature_creep_types per civ — Greek=Colossal+Shielded / Aztec=
    Beast+Spirit+Unarmored / Norse=Shielded+Unarmored)
  decisions/2026-05-06-per-tower-rn-cross-civ-audit-and-arc-close.md
    (45-tower roster type-coverage table)

§4.7 R2 PROCEDURE:
  1. Read R1 scope file + §4.7 current body + per-civ RN inputs.
  2. cascade-lint baseline.
  3. 3x debug loop on Direction A/B/C ratification (or fork rationale).
  4. File decisions/2026-05-06-enemy-direction-r2-direction-lock.md
     (Accepted, Medium). Cross-property coherence hook to R3 at close.
  5. cascade-lint, commit, dual-push (session branch + main).
     CRITICAL: targeted `git add <file>` only — never `git add -A`.
  6. Append PROGRESS session log entry (3-5 lines); compress oldest
     entry to PROGRESS-archive.md (keep 3 most-recent).
  7. CASCADE pointer single block (R1 archived to history); decisions
     table row added; footer 0.73 → 0.74 (0.71 already archived).
  8. PM autonomy mandate: proceed to R3 autonomously thereafter unless
     R2 reveals scope expansion or cascade-violation risk.

DOC-HYGIENE (per CLAUDE.md compression standard):
  - CASCADE decisions-table row: ONE LINE per round.
  - CASCADE current-work pointer: ~5 lines.
  - PROGRESS session log entry: 3-5 lines per round.
  - HANDOFF TL;DR: 3-5 sentences.

NO GAME CODE EDITS. Concept-only — Phase 4 hasn't exited.
SPINE-DOC EDITS deferred to RN. R2 + R3 are decision-file-only.
  concept/phase-4.md §4.7 untouched until RN per §-placement table.

VERIFY each commit with: python tools/cascade-lint.py
DUAL-PUSH each commit: session branch + main.
TARGETED `git add` ONLY — never `git add -A`.
```

---

## Escalation triggers

Hard-stop and flag if:

- §5.4 row beyond Civilizations would be touched.
- 2026-04-25 locked content-skeleton names would be modified.
- §2.4a is touched.
- Cultural-sensitivity concern surfaces (Follow-up #5 — scheduling locked at Phase-4 exit, but design-stage signal still warrants flag if Aztec / Norse cultural surface direction prose appears).
- Local `main` stale on session start.
- `cascade-lint` fails new (no carried findings — phase-4.md within 700 cap).
- Spine-doc edit at this arc would land before RN (R2 + R3 are decision-file-only per §-placement table).
- R2 attempts to fork direction without explicit reconciliation against all three evidentiary streams + Follow-up #5.
- §4.8 exit-gate item add/tick contemplated outside RN scope.
- The 17-item frame would be silently edited.
- Any Numbers-phase bound magnitude would be silently re-tuned.
- Any amendment-pass §-anchor would be silently edited.
- Any per-commander R1-R5 spine-doc surface would be silently edited.
- Any per-tower R1-RN magnitude row would be silently re-tuned out of band.
- Any per-civ R2/R3/R4 profile field or RN audit binding would be silently edited.
- §4.7 wave composition contradicts the per-civ RN matchup_affinity matrix or signature_creep_types without explicit reconciliation.
- Three-civ-three-equation-form fingerprint would be touched without supersession decision.
- Per-creep-row roster authoring is attempted (Phase 5 territory + Follow-up #5 blocker).
- A track touching CONCEPT constraints lands without 3x debug loop.
- `git add -A` is about to be invoked (use targeted staging instead).
- Context budget pressure forces a mid-track split → file mini-handoff and stop cleanly.

# PROGRESS — session-to-session task state

Source of truth for "what's done." Fresh session reads this **before** touching anything else.

**Approved plan:** `C:\Users\music\.claude\plans\i-want-to-prioritize-validated-dewdrop.md`
**Cadence plan:** `C:\Users\music\.claude\plans\prepare-for-next-stpes-shimmying-sphinx.md`

## How to read this file

- `[x]` = done (date + one-line artifact note).
- `[~]` = in-progress this session.
- `[ ]` = pending. **First unchecked box = next step.**

Each step follows the fixed loop: **propose → PM "go" → produce → verify → tick.** No batching.

## Workflow upgrade sequence

- [x] **Step 0** — install 5 skills (2026-04-19) — user-scope; reload reports 5 plugins / 2 skills / 14 agents loaded (skill-creator, frontend-design, session-report, plugin-dev, pr-review-toolkit).
- [x] **Step 1** — extract prototype data + PORT-NOTES stub (2026-04-19) — `prototype/data/{balance,towers,commanders}.json`, `prototype/PORT-NOTES.md` stub, `index.html` boot loader with inline fallback, `CASCADE.md` pointer bumped.
- [x] **Step 1.5a** — verify fetch path (2026-04-19) — `file://` CORS-blocked as expected (Python 3.14.3 present); shipped `prototype/start.bat` (Python `http.server` on :8765 + auto-open) and updated `prototype/README.md` with two-mode run instructions. Fallback still works; data-driven mode available via one double-click.
- [x] **Step 1.5b** — trim speculative PORT-NOTES rows (2026-04-20) — Audio + Telemetry rows removed; "Deliberately not here" section notes why and when they return. Audio architecture is committed at CONCEPT §3.10 but the Godot mapping only gets a row when the prototype or an engine sketch actually uses buses.
- [x] **Step 1.5c** — `hybrid.pair` clarification entry (2026-04-20) — [`decisions/2026-04-20-prototype-hybrid-pair-shape.md`](decisions/2026-04-20-prototype-hybrid-pair-shape.md) states the field is an ancestry tag, not a discovery rule. CONCEPT §4.3 blocker #1 remains open. Inline `_comment` in `towers.json` updated to cross-link.
- [ ] **Step 2** — [~skip] (optional) telemetry dump on match end → `localStorage.atoA_runs` + download button. *Default-skip per 2026-04-20 PM override session; revisit when Step 4 prototype pass lands and actual runs exist to capture.*
- [x] **Step 3** — `prototype/COVERAGE.md` landed (2026-04-20). CONCEPT × prototype matrix across Phases 1/2/3/4/5; legend ✅/🟡/⬜/OPEN; summary ranks top-5 Step-4 targets. Row 1 = Sinew commander (matches existing Step 4 default).
- [x] **Step 4** — second commander (Sinew-leaning) landed in prototype (2026-04-20). `commanders.json` — Commander A promoted to `playable: true` with epithet/lore/stats (passive: +15% Sinew tower damage; signature: "Warcry: next Sinew tower built this wave is +1 tier"). `index.html` Pick scene — new `.cmdr-roster` chip row above the main card; chips generated from `COMMANDERS.roster` filter `playable:true`; clicking a chip switches `COMMANDERS.active` + re-renders the full card. Display-only (passive/signature not wired to combat — matches prior Commander B pattern). [PROPOSAL] numbers per [`decisions/2026-04-20-commander-identity-floor.md`](decisions/2026-04-20-commander-identity-floor.md); flagged for PM revision post-playtest. COVERAGE row 1 closes: §5.1 MVP 1→2 commanders.
- [ ] **Step 5** — playtest findings → `decisions/` entries.
- [x] **Step 6** — PORT-NOTES sweep pass (2026-04-20). Status bumped Stub → Draft. Extended `commanders.json` row (playable flag + @export guidance for new fields: epithet/lore/stats.passive/stats.signature). Added Scenes row for Commander-roster chip chooser → `HBoxContainer` of `Button` with `ButtonGroup` selected-state. No Audio/Telemetry rows re-added (still out per Step 1.5b).
- [x] **Step 7** — Phase B research landed (2026-04-20). `research/00-methodology.md` Stub → Draft (rules doc; light editorial promotion). `research/03-commander-archetypes.md` Stub → Draft via research agent (~260 lines, ~12 citations majority Tier 1/2, all 6 sections populated with Loop-3 syntheses, archetype × lineage matrix table, [PROPOSAL] 5-slot roster-size recommendation + named caveats, explicit "NOT shipping" list). Tier 1/2 gaps disclosed in appendix (trope-fatigue causal claims, cognitive-load threshold, per-hero content-cost, some peer-title pick data). CASCADE rows 00 + 03 bumped Stub → Draft.
- [x] **Step 8** — Phase C stages landed (2026-04-20). `stages/00-session-start.md` Stub → Draft (212 lines; 11 sections; 5 new open questions with resolution venues; 6 measurable verification criteria). `stages/01-commander-pick.md` Stub → Draft (314 lines; 15 sections; 3 new open questions #8 no-commander/classic toggle, #9 silhouette read-test protocol, #10 return-from-match re-pick surface; 7 verification criteria inherit §4.1 identity floor). All [PROPOSAL] upstream inherited unlocked. Upstream soft spot flagged: stage-00 tutorial-to-match auto-start encoding (lean, not asserted — Stage 01 owns); CONCEPT §3.2 "color" treated as derivative of lineage affinity, not standalone floor slot. CASCADE rows 00 + 01 bumped Stub → Draft.
- [x] **Step 10** — Prototype concept-demo pivot (2026-04-20). Ratified via [`decisions/2026-04-20-prototype-concept-demo-pivot.md`](decisions/2026-04-20-prototype-concept-demo-pivot.md). Plan at `C:\Users\music\.claude\plans\glittery-fluttering-wilkinson.md`. Freeze date: 2026-05-15. All four phases landed in single autonomous session.
  - [x] **10.1** — Scope pivot ratification + data expansion (2026-04-20). Decision entry [`2026-04-20-prototype-concept-demo-pivot.md`](decisions/2026-04-20-prototype-concept-demo-pivot.md) filed with 3x debug loop. Data files landed: [`ages.json`](prototype/data/ages.json) (new, 11 rows with era-descriptor + color + signature motif + lore), [`towers.json`](prototype/data/towers.json) (5 lineages × 11 ages = 55 derived rows; 5 base archetypes Rampart/Kiln/Smeltery/Standard/Glamour + 5 hybrid entries on adjacency ring Sinew↔Ember↔Forge↔Crown↔Veil↔Sinew), [`commanders.json`](prototype/data/commanders.json) (A/B/C/D/E all `playable: true`; each has lore + stats + 12 VO text-captions + progression block), [`enemies.json`](prototype/data/enemies.json) (new, per-age archetype from Pelt-Runner to Light-Mote), [`balance.json`](prototype/data/balance.json) (coop/multiplayer/save/progression/accessibility sub-objects). JSON validation passes; cascade-lint clean. All numbers [PROPOSAL].
  - [x] **10.2** — Scene rebuild + first-run/returning flow + identity-floor visuals (2026-04-20). `prototype/index.html` full rewrite. New scenes: splash (3s auto-advance, skippable, reduce-motion aware), login (localStorage `atoA.profile.v1`), menu (greeting + Continue/Play/Commander/Options/Logout/Quit), mode-select (Skirmish/Tutorial/Co-op Horde/Campaign-stub cards), lobby-coop, commander-pick (5 lineage-colored initial-letter portraits, XP progress bar, 3 cosmetic slots at L5/L10/L15), options (5 tabs — Audio/Input/Video/Accessibility/Gameplay with live-preview sliders), pause overlay (Esc, mode-aware: Resume/Options/Restart/Save-and-Exit/Quit). Profile namespace wraps localStorage with login/logout/settings/writeMatchSave. Send-Wave button disabled during `waveActive` + wave-indicator label "Wave N in progress". Keyboard shortcuts (Space=send-wave, A=age-up, 1–5=select tower). All 5 lineage tower buttons + adjacent hybrid. Colorblind glyph toggle draws letter labels on towers. Subtitle captions for commander VO pick/victory/defeat lines. UI scale slider 75–150%. XP awarded on match-end (40 win / 12 loss) with auto-level-up toasts.
  - [x] **10.3** — Co-op Horde multiplayer via PeerJS (2026-04-20). CDN `peerjs@1.5.4` loaded. `Net` module handles host/join/handshake via `new Peer()` → DataChannel. Host broadcasts `{type:'snapshot', payload}` at 10Hz with full game state (wave/gold/knowledge/ageIdx/lives/towers/enemies/shots). Guest sends `{type:'intent'}` with place/send-wave/age-up/chat; host drains intents each tick via `processRemoteIntents()`. Guest-owned towers tagged with `owner:'guest'` render with dashed white border. Chat panel with 100-char limit + system messages on connect/disconnect. 10s reconnect window on disconnect → match aborts and returns to menu. `?join=<peerID>` URL param auto-dials after splash/login. Net status badge in match board (Connected/Degraded/Lost). Host broadcasts match-end `{type:'match-end', result}` to sync end screens.
  - [x] **10.4** — Polish + verification (2026-04-20). `tools/cascade-lint.py` extended with `check_prototype_data()` validating JSON schemas for balance/ages/towers/commanders/enemies + 11-age cardinality check + all-playable-commander check. Lint runs clean. `prototype/README.md` rewritten with v2 changelog entry, concept-demo scene table, Login/Logout instructions, and step-by-step Co-op Horde instructions (host invite code + `?join=` URL auto-dial). Keyboard navigation live (Esc pauses match / closes options / returns to menu; Space/A/1–5 shortcuts in match). Tutorial Skip button returns to menu. All changes respect CLAUDE.md "port findings, not code" discipline via PORT-NOTES continuity.
- [x] **Step 9** — cascade lint + fresh HANDOFF landed (2026-04-20). [`tools/cascade-lint.py`](tools/cascade-lint.py) checks CASCADE links / stage+research Status+Last-reviewed headers / decision Reversibility / decisions-table row-count parity / PROGRESS first-unchecked. Runs clean; reports `next-open-step: Step 2` (telemetry, default-skipped). HANDOFF.md rewritten with second-override TL;DR + state snapshot + open blockers + next-session checklist including `python tools/cascade-lint.py` run-first step.

## Debts carried

- **Playtest findings — all three filed 2026-04-20 as dated `decisions/` entries (see below):**
  - **Finding #1** — tutorial glowing-cell affordance too subtle → [`decisions/2026-04-19-tutorial-cell-affordance.md`](decisions/2026-04-19-tutorial-cell-affordance.md). Acceptance criteria: Step 4 prototype pass must pass a grayscale screenshot check.
  - **Finding #2** — intro-baseline gaps (no settings/pause/skip/audio/save/a11y) → [`decisions/2026-04-19-intro-baseline-gaps.md`](decisions/2026-04-19-intro-baseline-gaps.md). Proposal layer produced: [`CONCEPT-GAPS.md`](CONCEPT-GAPS.md).
  - **Finding #3 (meta)** — concept thinness → [`decisions/2026-04-19-concept-thinness-escalation.md`](decisions/2026-04-19-concept-thinness-escalation.md). Workflow queue paused; resumption criteria in that entry.
- **New debt (inserted this pass):** PM reads `CONCEPT-GAPS.md`, picks top-5 (or different five) to ratify. Each ratified row → its own decision entry + per-phase CONCEPT.md amendment. Until that happens, queue stays paused.
- **2026-04-20 resolution (PM-overridden cadence batch):** top-5 RATIFIED. Five decision entries filed (meta-UI envelope, audio architecture, accessibility floor, commander identity floor, first-run flow). CONCEPT.md amended across §2.4a / §3.2 / §3.9 / §3.10 / §3.11 / §4.1 / §5.1 / §6.4. CONCEPT-GAPS migrated 14 of 26 rows; 13 remain. Queue stays paused pending PM review of this batch + direction on remaining 13 rows.
- **2026-04-20 (next-session-continuation, second PM cadence override):** batch accepted as-is. SAVE-01+SAVE-02 ratified via [`2026-04-20-save-model.md`](decisions/2026-04-20-save-model.md); CONCEPT.md §4.9 added; CONCEPT-GAPS migrated 2 more rows (16/26 total, 11 remain). PROGRESS Steps 1.5b + 1.5c landed in the same sweep (PORT-NOTES trim + `hybrid.pair` clarification entry). Queue advances to Step 2 (optional telemetry — default skip) / Step 3 (COVERAGE matrix).
- **2026-04-24 Phase 1 reopen debts (new):**
  - **Step A §-edits in-tree as superseded-record** — the 18+ §-edits landed across `concept/phase-1..7.md`, `CONCEPT.md` hub, `README.md` #3, and stage stub-amends under 2026-04-21 are no longer Accepted as Phase 1 truth, but they are NOT force-reverted. They remain as raw material for the reopen and must be re-ratified, rewritten, or explicitly retired as Phase 1 exits.
  - **Three 2026-04-20 decisions un-rebased** — [`commander-identity-floor`](decisions/2026-04-20-commander-identity-floor.md), [`age-history-flavor-mapmods`](decisions/2026-04-20-age-history-flavor-mapmods.md), [`commander-pick-identity-upgrade`](decisions/2026-04-20-commander-pick-identity-upgrade.md) each carry a 2026-04-21 "rebased by" banner that now points at a Superseded parent. DO NOT force-revert or silently re-rebase. Reconcile after Phase 1 exits per reopen Follow-ups #7.
    - **2026-04-26 annotation: CLOSED.** Re-rebase banners dated 2026-04-26 filed inline on all three decisions, pointing at the 2026-04-25 real-cultures ratification (live parent). Re-rebase, not supersede — Accepted status preserved; identity-floor shape / banner mechanic / silhouette-test harness all survive intact per 2026-04-25 Follow-up #10.
  - **Step B void** — [`2026-04-22-step-b-prototype-reshape-plan.md`](decisions/2026-04-22-step-b-prototype-reshape-plan.md) was Proposed; its 18-sub-step 3/3/3 migration is now superseded. Any prototype reshape is downstream of Phase 1 exit, not upstream of Step 5.
  - **Working-title "Ash to Altar"** no longer thematically load-bearing. Remains working-only; Q8 addresses near end of reopen sequence.
  - **Phase 1 exit gate (new):** PM answers in one page — *what the world is / Commander's role / hybrid topology meaning*. Until that page exists, Phase 1 stays open and all downstream reconciliation waits.
    - **2026-04-25 annotation:** rubric is **substantively answered** by [`decisions/2026-04-25-q2-real-cultures-direction-ratified.md`](decisions/2026-04-25-q2-real-cultures-direction-ratified.md) (Real cultures frame: Greek/Aztec/Norse civs + commanders + 18 T1-T3 towers + 15 units + 18 T4 Demigods + 9 Gods + Tribute/Divinity economy + 30-round match arc). Only the **formal one-page write-up** is still owed; tracked as Follow-up #11 in the ratification decision. Downstream reconciliation (CONCEPT.md + concept/phase-*.md + stage docs + README cascade) unblocked and queued.
    - **2026-04-26 annotation: CLOSED.** Formal one-page write-up filed as [`decisions/2026-04-26-phase-1-exit-one-pager.md`](decisions/2026-04-26-phase-1-exit-one-pager.md) (Accepted; Reversibility Easy).
- **2026-04-26 admin/concept.json staleness (new debt):** [`admin/concept.json`](admin/concept.json) still reflects the pre-2026-04-21 5-lineage / 11-age shape (Sinew/Ember/Forge/Crown/Veil; Primal→Ascendant; 5 hybrid entries). NOT migrated through 2026-04-21 3/3/3 and NOT migrated through 2026-04-25 real-cultures shape. Staleness notice added to `meta.staleNotice` in the file on 2026-04-26 so the admin UI surfaces the warning. **Migration pending PM direction** — options are (a) full rewrite to 3-civ/4-tier/Fusion shape as a dedicated pass, (b) regenerate from CONCEPT.md head by extending the admin/ tooling, or (c) retire admin/concept.json in favor of concept/phase-*.md as sole source of truth. Source of truth for now is the CONCEPT doc tree + 2026-04-25 ratification + 2026-04-26 one-pager; do NOT trust admin/concept.json counts/nodes/edges.

## Watch-items

- **Skills ROI probation.** If `skill-creator` has not been invoked by end of Step 5, `/plugin uninstall plugin-dev` and `/plugin uninstall frontend-design` to recover context-window. Bright line; don't forget.

## Handoff trigger

When PM types **"prepare for handoff"** (or: "handoff time", "wrap session", "checkpoint"):
1. Claude re-reads `PROGRESS.md`, `CASCADE.md`, concept.json head, BALANCE region, `decisions/` listing.
2. Claude rewrites `HANDOFF.md` TL;DR + state snapshot + carried debts/blockers.
3. Claude bumps `CASCADE.md` pointer if doc status changed.
4. Claude outputs one fenced copypastable prompt block for the next session after `/clear`.
5. Session stops — no new step started.

## Session log

- **2026-05-01 (C7.b LANDED: cast-emerge pipeline + BuilderUnit class, two slices)** — Per PM "go" gates. **Slice 1 (commit `ae28e1d`):** restored `commander: { castState, castTarget, castTtl }` in `initMatch`; added `CAST_DURATIONS = { short: 72, long: 168, signature: 270 }` (frames @ 60Hz ~= ~1.2s/~2.8s/~4.5s per 2026-04-27 spec). Hook in `castAbility(slot)`: sets `game.commander.castState/castTarget/castTtl` for the selected tier (long → signature). Tick decrements ttl + clears state at zero. Render: civ-paletted `renderSilhouette()` 1.4× at `game.path[0]` while cast active; signature outer pulsing ring; reduce-motion → gold ring pulse only. Net snapshot includes `commander: { castState, castTarget, castTtl }`; guest applies all three fields. **Slice 2 (commit `817d048`):** added `builders: []` array to `initMatch` state (after commander struct); spawn hook in `placeTower` pushes `{x, y, target:{x,y}, ttl:120, totalTtl:120, civ, tier:1}` from path[0] toward placed cell center. Tick lerps x/y by `1 - (ttl/totalTtl)`, decrements ttl, filters out ttl<=0. Render draws small 4px dot + 7px accent ring per builder, civ-tinted via `CIV_PALETTE` or neutral slate `{primary:"#94a3b8", accent:"#cbd5e1"}` for legacy commander-a..e (no `civ` field). Snapshot build slices to 32 + applies on guest. **Deferred:** Builder concurrency cap, 90% refund-on-cancel (no cancel UI surface yet). cascade-lint clean throughout. C7.b complete across both slices; dual-pushed.
- **2026-05-01 (C7.a LANDED: gut persistent on-field commander avatar, alongside, comment-out)** — Per PM "go" on Recommended option. 12 commander-avatar surfaces commented-out (NOT deleted) with dated `// SUPERSEDED 2026-04-27 (C7.a)` markers preserving full code as raw material for C7.b/C6: `initMatch` commander state line + 5-field game.commander struct; KeyQ + KeyC handlers; net commander-move/-sig handlers (`if (false &&...)`); net snapshot build sets `commander: null` + apply block wrapped; `onBoardClick` Shift+click branch; `commanderAuraEffectOn` / `moveCommanderTo` / `fireCommanderSignature` function bodies; `startWave` movedThisWave reset; tick-block (cd timer + knock-back); render-block (aura + avatar). `towerStats` aura mul replaced with neutral identity values (`{damage:1, cd:1, range:1, active:false}`) so tower math stays unaffected. cascade-lint clean. Commit `6063520` + dual-push.
- **2026-05-01 (C5 LANDED: Codex modal — 7×5 attack-type vs armor RPS matrix, alongside)** — Per PM "go" on Recommended option (codex-modal, pure-additive — no tower-card collision). Tutorial + match HUD each gain 📖 Codex button → opens `.codex-modal` with `<table class="cx-grid">`: 7 attack-type rows × 5 armor-tag columns; cells colored `.strong` (+25%, green #16a34a) / `.weak` (-25%, red #b91c1c) / `.neutral` (1.0×); status-proc legend below grid; +25%/-25% bonus/penalty footnote. Reads inlined `ATTACK_TYPES` constant (7 types: Piercing/Slashing/Crushing/Fire/Poison/Arcane/Divine) sourced verbatim from [`prototype/data/attack-types.json`](prototype/data/attack-types.json). `openCodex()` / `closeCodex()` + Esc handler. ~70 lines JS + 24 lines CSS, all alongside; no balance wiring, no tower-card mutation. cascade-lint clean. Commit `95165b9` + dual-push.
*Older entries archived to [`PROGRESS-archive.md`](PROGRESS-archive.md).*
